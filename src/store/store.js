import { createStore } from 'vuex';
import router from '@/router/router';

export const store = createStore({
	state: () => ({}),
	mutations: {},
	getters: {},

	/* modules: {
		questsAndEvents: questsAndEventsModule,
		auth: authModule,
		administration: administrationModule,
		startQuiz: startQuizModule,
	}, */
});
